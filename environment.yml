name: finmpy
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pandas>=2.0.0
  - polars>=0.20.0
  - numpy>=1.24.0
  - psutil>=5.9.0
  - pytest>=7.0.0
  - typing-extensions>=4.5.0
  - pip
  - pip:
      # Optional — if you ever add visualization or reporting
      - matplotlib>=3.8.0
      - plotly>=5.20.0
